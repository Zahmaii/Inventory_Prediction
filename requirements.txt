# Specifying a compatible Python version for deployment
# Ensure the runtime is Python 3.9 or 3.10 on your deployment platform
python-3.9

# Update pip to the latest version to avoid issues
pip>=24.3.1

# Install setuptools to handle distutils errors
setuptools>=65.0.0

# Install a compatible version of pandas with Streamlit 1.20.0
pandas==1.5.3

# Install matplotlib for plotting
matplotlib==3.7.1

# Install Streamlit with a compatible version of pandas
streamlit>=1.24.0

# Install scikit-learn for machine learning
scikit-learn==1.3.0

# Install the option menu for Streamlit app
streamlit-option-menu==0.3.0

# Install TensorFlow CPU version compatible with your environment
tensorflow-cpu==2.10.0

# Download the Program Requirements File by
# Copy and Paste the Text Below to your Environment Terminal
# Remove the "#"
# pip install -r requirements.txt

# Run the program by Typing the Below Text
# streamlit run izi.py
# Remove the "#"
